{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.c|*.h) command -v clang-format >/dev/null 2>&1 && clang-format -i \"$f\";; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
